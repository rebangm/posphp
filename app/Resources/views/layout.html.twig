{% extends "::base.html.twig" %}

{% block body %}
<div class="navbar navbar-inverse navbar-fixed-top">

        <div class="container">
            <button type="button" class="btn btn-default navbar-btn navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div class="navbar-collapse collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="{{path('pos_home_homepage')}}">Home</a></li>
                    <li><a href="{{path('pos_user_manage')}}">User</a></li>
                    <li><a href="{{path('pos_customer_manage')}}">Customer</a></li>
                    <li><a href="#contact">Contact</a></li>
                    
                </ul>

                <form class="navbar-form navbar-right">
                    <a href="{{ path('pos_user_logout') }}" class="btn btn-default">logout</a>
                </form>
                <div class="navbar-right"><p class="user-info"><i class="glyphicon glyphicon-user"></i> Welcome {{ app.user.username }}</p></div>
            </div><!--/.nav-collapse -->
        </div>

</div>

<div class="container">
    <div class="row">
        <div id="leftMenu" class="col-md-2">
            <a class="posLogo"href="#"><img src="/img/Logo_POS.jpg" /> </a>
            {% block leftmenu %}
                <h3>Mini-ERP</h3>
                <ul class="nav nav-pills nav-stacked">
                    <li><a href="{{ path('pos_home_homepage') }}">Accueil de l'application</a></li>
                </ul>
            {% endblock %}
        </div>
        <div id="content" class="col-md-10">
            {% block content %}
            {% endblock %}
        </div>
    </div>

    <hr>
</div>
{% endblock %}
