{% extends "PosProductBundle::layout.html.twig" %}


{% block content %}

    <h3>welcome to manage product</h3>

    {% include "PosProductBundle::alert.html.twig" %}

    <table id="userTable" class="table table-striped table-hover table-condensed">
        <thead>
        <tr><td class="">id</td><td>Nom</td><td>barcode</td><td>action</td></tr>
        </thead>
        <tbody>
        {% for product in pagination %}
            <tr><td>{{supplier.id}}</td>
                <td>{{supplier.name}}</td>
                <td>
                   <a href="{{ path('pos_supplier_manage_view', {'supplier_id': supplier.id }) }}" class="btn btn-default btn-xs"><i class="glyphicon glyphicon-eye-open"></i></a>
                   <a href="{{ path('pos_supplier_manage_edit', {'supplier_id': supplier.id }) }}" class="btn btn-default btn-xs"><i class="glyphicon glyphicon-edit icon-edit"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {{ knp_pagination_render(pagination) }}
{% endblock %}
