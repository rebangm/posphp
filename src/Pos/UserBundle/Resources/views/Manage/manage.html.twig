{% extends "PosUserBundle::user.html.twig" %}
 
{% block content %}
 
    <h3>welcome to manage user</h3>

    {% include "PosUserBundle::alert.html.twig" %}

    <table id="userTable" class="table table-striped table-hover table-condensed">
        <thead>
            <tr><td class="">id</td><td>Utilisateur</td><td>Nom</td><td>Pr&eacute;nom</td><td>Actif</td><td>action</td></tr> 
        </thead>
        <tbody>

        {#
            {% for user in users %}
                <tr><td>{{user.id}}</td>
                    <td>{{user.username}}</td>
                    <td>{{user.lastname}}</td>
                    <td>{{user.firstname}}</td>
                    {% if user.isActive %}
                        <td><button data-url='{{ path('pos_user_manage_ajax_active', {'id': user.id, 'active': 0 }) }}'  class="btn btn-success"><i class="icon-ok icon-white"></i></button></td>
                    {% else %}
                        <td><button data-url='{{ path('pos_user_manage_ajax_active', {'id': user.id, 'active': 1 }) }}'  class="btn btn-danger"><i class="icon-remove icon-white"></i></button></td>
                    {% endif %}
                    <td>
                        <a href="{{ path('pos_user_manage_edit', {'id': user.id }) }}" class="btn"><i class="icon-edit"></i></a>
                        <a href="{{ path('pos_user_manage_edit', {'id': user.id}) }}" class="btn"><i class="icon-align-left"></i></a>
                </tr>
            {% endfor %}
        #}

            {% for user in pagination %}
                <tr {% if loop.index odd %} class=""{% endif %}><td>{{user.id}}</td>
                    <td>{{user.username}}</td>
                    <td>{{user.lastname}}</td>
                    <td>{{user.firstname}}</td>
                    {% if user.isActive %}
                        <td><button data-url='{{ path('pos_user_manage_ajax_active', {'id': user.id, 'active': 0 }) }}'  class="btn btn-success"><i class="icon-ok icon-white"></i></button></td>
                    {% else %}
                        <td><button data-url='{{ path('pos_user_manage_ajax_active', {'id': user.id, 'active': 1 }) }}'  class="btn btn-danger"><i class="icon-remove icon-white"></i></button></td>
                    {% endif %}
                    <td>
                        <a href="{{ path('pos_user_manage_edit', {'id': user.id }) }}" class="btn"><i class="icon-edit"></i></a>
                        <a href="{{ path('pos_user_manage_edit', {'id': user.id}) }}" class="btn"><i class="icon-align-left"></i></a>
                </tr> 
            {% endfor %}
        </tbody>
    </table>
 
    {% include "PosUserBundle::pagination.html.twig" %}
{% endblock %}

