{% extends 'form_div_layout.html.twig' %}

{% block form_label %}
   <div class="form-label">
    {{ parent() }}

    {% if required %}
        <span class="required-field" title="Ce champ est obligatoire">*</span>
    {% endif %}
   </div>
{% endblock %}

{#
{% block form_row %}
    {% spaceless %}
        <div>
            {{ form_label(form) }}
            {{ form_errors(form) }}
            {{ form_widget(form) }}
        </div>
    {% endspaceless %}
{% endblock form_row %}
#}